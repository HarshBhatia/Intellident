# Files & X-rays

Removed from MVP. On `archive/pre-mvp-split` this is the patient-detail
Files tab stub plus unused `handleFormXRayUpload` in PatientDetailClient.

Resume by restoring that tab (`flags.filesXrays`) and the visit x-ray
upload UI from `archive/pre-mvp-split`.
